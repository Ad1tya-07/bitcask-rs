[package]
name = "storage_engine"
version = "0.1.0"
edition = "2024"

[dependencies]
crc32fast = "1.5.0"

[dev-dependencies]
criterion = "0.5"

[[bench]]
name = "sync_modes"      # matches benches/sync_modes.rs
harness = false          # <-- important: disables the default bench harness so criterion's takes over